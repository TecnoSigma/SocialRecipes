<h1><strong>Cadastro de Receitas</strong></h1>
<%= form_for(@cuisine) do |f| %>
  <% if @cuisine.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(@cuisine.errors.count, "Erro") %>: *Preenchimento obrigatório!</h2>
      <ul>
      <% @cuisine.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>
  <label>Lista de Cozinhas Cadastradas</label><br>
  <select size="10">
    <%= @list_cuisine.sort.each do |c| %>
      <option><%= c %></option>
    <% end %>
  </select>
  <br>
  <br>
  <label>Nome da cozinha*</label><br>
  <%= f.text_field :name %><br><br>
  <button type="submit">Salvar Cozinha</button>
<% end %>